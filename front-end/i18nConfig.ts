const i18nConfig = {
  locales: ["en", "fr", "ar"],
  defaultLocale: "en",// the default will be in 
  // prefixDefaultLocale: true, // this will prefix the default locale to the url
  
};

export default i18nConfig;
